# Ansible Vault Password Hint
# The vault password is required to decrypt group_vars/all/vault.yml
#
# Usage:
#   ansible-playbook playbooks/smb-shares.yml --ask-vault-pass
#   ansible-playbook playbooks/smb-shares.yml --vault-password-file=~/.vault_pass
#
# To edit the vault:
#   ansible-vault edit group_vars/all/vault.yml
#
# To view the vault:
#   ansible-vault view group_vars/all/vault.yml
